
    <tr>
      <td>  <%= item.status %>  </td>
      <td> <%= item.description %> </td>
      <td> <%= item.category.name %> </td>
      <td> <%= link_to "Edit", edit_item_path(item) %> </td>
    </tr>

